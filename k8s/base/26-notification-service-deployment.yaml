apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: fin-account-hub
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: uniquecolor/notification-service:1.0.0
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: fin-account-config
            - secretRef:
                name: fin-account-secrets
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mariadb://mariadb-notification:3306/appdb"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5