spring:
  cloud:
    config:
      uri: http://config-service:8888
      name: eureka-server
      fail-fast: true
      retry:
        initial-interval: 2000
        max-attempts: 6
        multiplier: 1.5