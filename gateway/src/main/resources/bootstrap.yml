spring:
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://config-service:8888}
      name: gateway
      fail-fast: true
      retry:
        initial-interval: 2000
        max-attempts: 6
        multiplier: 1.5